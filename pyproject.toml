[project]
name = "fork-fit"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    "python-dotenv",
    "bcrypt",
    "PyJWT",
    "motor",
    "groq",
]

[tool.vercel]
entrypoint = "backend.server:app"